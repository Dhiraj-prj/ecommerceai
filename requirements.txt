# Production-optimized: Pre-compiled wheels only!
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0
sqlalchemy==2.0.35
python-multipart==0.0.9
jinja2==3.1.4
scikit-learn==1.3.2  # OLDER = Pre-compiled wheels!
pandas==2.0.3        # OLDER = Pre-compiled wheels!
numpy==1.24.4        # FIXED versions = NO COMPILATION!
